export default class City {
  constructor(id, name, temp, weather, hight, low, wind, icon) {
    this.id = id;
    this.name = name;
    this.temp = temp;
    this.weather = weather;
    this.high = high;
    this.low = low;
    this.wind = wind;
    this.icon = icon;
  }
}
